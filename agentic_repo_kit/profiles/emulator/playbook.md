## Emulator investigation loop

Reproduce the title-visible symptom, reduce it to an emulator subsystem/semantic question, instrument both guest-facing state and host implementation state, compare against the intended guest contract or independent evidence, then decide whether the change is a generic correctness fix, guarded fast path, title-specific workaround, or only a diagnostic.
