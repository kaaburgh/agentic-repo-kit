{{MARKER}}
# Agent playbook

This playbook turns the repository policy into an execution loop for **{{PROJECT_NAME}}**.

Cloud-first execution is **{{CLOUD_FIRST}}**. One roadmap item per PR is **{{ONE_ITEM_PER_PR}}** for roadmap-scoped feature or investigation work; a process-only PR may omit a roadmap item when it explicitly explains why no roadmap item applies.

Before roadmap-scoped work, read `AGENTS.md`, the selected roadmap item, its dependencies, and linked durable evidence. For process-only work, read `AGENTS.md` and the relevant durable policy/docs without inventing an unrelated roadmap item. Prefer a bounded experiment or implementation whose result can be reviewed without chat history.

{{PLAYBOOK_SECTIONS}}
