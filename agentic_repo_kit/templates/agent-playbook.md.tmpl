{{MARKER}}
# Agent playbook

This playbook turns the repository policy into an execution loop for **{{PROJECT_NAME}}**.

Cloud-first execution is **{{CLOUD_FIRST}}**. One roadmap item per PR is **{{ONE_ITEM_PER_PR}}**.

Before work, read `AGENTS.md`, the selected roadmap item, its dependencies, and linked durable evidence. Prefer a bounded experiment or implementation whose result can be reviewed without chat history.

{{PLAYBOOK_SECTIONS}}
